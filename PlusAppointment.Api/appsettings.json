{
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://*:8080"
      }
    }
  },
  "TextMagic": {
    "Username": "oauthhieplecoding",
    "ApiKey": "${TEXTMAGIC_API_KEY}"
  },
  "AWS": {
    "Region": "${AWS_REGION}",
    "AccessKey": "${AWS_ACCESS_KEY}",
    "SecretKey": "${AWS_SECRET_KEY}",
    "ServiceURL": "${AWS_SERVICE_URL}",
    "SQSQueueUrl": "${AWS_SQS_QUEUE_URL}"
  },
  "Jwt": {
    "Key": "${JWT_KEY}"
  },
  "ConnectionStrings": {
    "DefaultConnection": "${DB_CONNECTION_STRING}",
    "RedisConnection": "${REDIS_CONNECTION_STRING}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
